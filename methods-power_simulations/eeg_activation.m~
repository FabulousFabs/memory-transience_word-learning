function [d, t] = eeg_activation(L, Fs, f, A, p)
    s = 1 / Fs;
    t = 0:s:(L-s);
    d = zeros(size(t));
    
    for i = 1:size(p, 1)
        d_t = 0:s:p(i, 2
        d_s = A .* sin(2 * pi * f .* t
    end
end